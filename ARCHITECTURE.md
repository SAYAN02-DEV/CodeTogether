# 🏗️ System Architecture

## Complete System Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                                                                   │
│  ┌────────────────┐                                              │
│  │   MetaMask     │ ◄─── Signs messages to prove identity        │
│  │   Wallet       │                                              │
│  └────────────────┘                                              │
│         │                                                         │
│         ▼                                                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              React Frontend (Vite + TypeScript)          │   │
│  │                                                           │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │   │
│  │  │ RainbowKit   │  │    Monaco    │  │   shadcn/ui  │  │   │
│  │  │   (Wallet    │  │   (Editor)   │  │ (Components) │  │   │
│  │  │  Connection) │  │              │  │              │  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │   │
│  │                                                           │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │         wagmi + viem (Web3 Integration)          │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP API Calls
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Express Backend (Node.js)                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    API Endpoints                          │   │
│  │                                                           │   │
│  │  • POST /api/projects/ipfs                               │   │
│  │    └─► Save project with signature verification          │   │
│  │                                                           │   │
│  │  • GET /api/projects/wallet/:address                     │   │
│  │    └─► Get all projects for a wallet                     │   │
│  │                                                           │   │
│  │  • GET /api/projects/ipfs/:cid                           │   │
│  │    └─► Get project by IPFS CID                           │   │
│  │                                                           │   │
│  │  • POST /api/projects/verify-access                      │   │
│  │    └─► Verify user has access to project                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         ethers.js - Signature Verification               │   │
│  │  • verifyMessage(message, signature)                     │   │
│  │  • Ensures wallet ownership                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │      In-Memory Storage (Projects Metadata)               │   │
│  │  • projectName, cid, walletAddress                       │   │
│  │  • signature, timestamp, fileCount                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ Upload Files
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  IPFS Network (web3.storage)                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Decentralized Storage Network                     │   │
│  │                                                           │   │
│  │  • Files stored across multiple nodes                    │   │
│  │  • Content-addressed (CID = hash of content)             │   │
│  │  • Immutable (changes = new CID)                         │   │
│  │  • Accessible via IPFS gateways                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  Gateway URL: https://w3s.link/ipfs/{CID}                       │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ Anyone can access
                            ▼
                    ┌───────────────┐
                    │  Public Web   │
                    └───────────────┘
```

## Data Flow: Saving a Project

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1: User Creates Project                                    │
├─────────────────────────────────────────────────────────────────┤
│ • User writes code in Monaco editor                             │
│ • Files stored in React state                                   │
│   Example:                                                       │
│   - src/App.tsx (code content)                                  │
│   - src/index.css (styles)                                      │
│   - package.json (metadata)                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: Click "Save to IPFS"                                    │
├─────────────────────────────────────────────────────────────────┤
│ • SaveToIPFSDialog opens                                        │
│ • Shows list of files to upload                                 │
│ • User reviews and confirms                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Request Wallet Signature                                │
├─────────────────────────────────────────────────────────────────┤
│ • useWalletAuth.verifyOwnership() called                        │
│ • Creates message:                                              │
│   "CodeSync - Verify ownership of wallet: 0x1234..."           │
│   "Timestamp: 1700000000000"                                    │
│ • MetaMask popup appears                                        │
│ • User signs message (FREE - no gas!)                           │
│ • Returns: { signature, message, address }                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Upload to IPFS                                          │
├─────────────────────────────────────────────────────────────────┤
│ • uploadToIPFS(files, projectName) called                       │
│ • Files converted to File objects:                              │
│   new File([blob], 'src/App.tsx')                              │
│ • Uploads to web3.storage via w3up-client                       │
│ • Returns:                                                       │
│   {                                                              │
│     cid: "bafybeiabc123...",                                    │
│     url: "https://w3s.link/ipfs/bafybeiabc123..."              │
│   }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Save to Backend                                         │
├─────────────────────────────────────────────────────────────────┤
│ • POST /api/projects/ipfs                                       │
│ • Payload:                                                       │
│   {                                                              │
│     projectName: "my-project",                                  │
│     cid: "bafybeiabc123...",                                    │
│     walletAddress: "0x1234...",                                 │
│     signature: "0xabcd...",                                     │
│     message: "verification message",                            │
│     fileCount: 5                                                │
│   }                                                              │
│                                                                  │
│ • Backend verifies signature:                                   │
│   recovered = verifyMessage(message, signature)                 │
│   if (recovered !== walletAddress) reject!                      │
│                                                                  │
│ • Stores in memory:                                             │
│   projectId = wallet-timestamp                                  │
│   ipfsProjects.set(projectId, {...data})                        │
│                                                                  │
│ • Returns success + CID                                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Show Success                                            │
├─────────────────────────────────────────────────────────────────┤
│ • Dialog shows success message                                  │
│ • Displays:                                                      │
│   - IPFS CID (copy to clipboard)                               │
│   - Gateway URL (click to view)                                │
│ • Project card gets IPFS badge                                  │
│ • Files are now on decentralized web!                           │
└─────────────────────────────────────────────────────────────────┘
```

## Security Flow

```
┌──────────────────────────────────────────────────────────────┐
│              CRYPTOGRAPHIC SECURITY CHAIN                     │
└──────────────────────────────────────────────────────────────┘

1. Wallet Address (Public Key)
   ↓
   Example: 0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb7

2. Message to Sign
   ↓
   "CodeSync - Verify ownership of wallet: 0x742d35..."
   "Timestamp: 1700000000000"

3. User Signs with Private Key (in MetaMask)
   ↓
   Signature: 0x1234abcd... (only wallet owner can create this)

4. Frontend Sends to Backend
   ↓
   { walletAddress, message, signature }

5. Backend Verifies
   ↓
   recoveredAddress = ethers.verifyMessage(message, signature)
   
   if (recoveredAddress === walletAddress) {
     ✅ VERIFIED - User owns this wallet
   } else {
     ❌ REJECTED - Fake or tampered signature
   }

6. Store with Proof
   ↓
   Project linked to wallet address
   Signature stored as proof of ownership
```

## IPFS Content Addressing

```
┌──────────────────────────────────────────────────────────────┐
│                    HOW IPFS WORKS                             │
└──────────────────────────────────────────────────────────────┘

Traditional Web (Location-based):
   URL: https://example.com/project/files.zip
        └─► Server location (can change, can go down)

IPFS (Content-based):
   CID: bafybeiabc123...
        └─► Hash of content (permanent identifier)

┌─────────────────────────────────────────────────────────┐
│ Your files: [App.tsx, index.css, package.json]        │
└─────────────────────────────────────────────────────────┘
                      ↓ Hash
┌─────────────────────────────────────────────────────────┐
│ CID: bafybeiabc123def456...                            │
│ (Unique fingerprint of content)                        │
└─────────────────────────────────────────────────────────┘
                      ↓ Store
┌─────────────────────────────────────────────────────────┐
│ IPFS Network (distributed across many nodes)           │
│ • Japan: Has copy                                       │
│ • USA: Has copy                                         │
│ • Europe: Has copy                                      │
│ • Cannot be taken down!                                 │
└─────────────────────────────────────────────────────────┘
                      ↓ Access
┌─────────────────────────────────────────────────────────┐
│ Gateway URL: https://w3s.link/ipfs/bafybeiabc123...    │
│ Anyone in the world can access                          │
└─────────────────────────────────────────────────────────┘

Key Properties:
✅ Same content = Same CID (always)
✅ Different content = Different CID
✅ Immutable (can't change without new CID)
✅ Verifiable (hash proves authenticity)
✅ Permanent (distributed storage)
```

## Component Hierarchy

```
App.tsx
├─ WagmiProvider (Web3 context)
│  └─ RainbowKitProvider (Wallet UI)
│     └─ QueryClientProvider (React Query)
│        └─ Router
│           ├─ Home.tsx
│           │  ├─ AppHeader
│           │  │  └─ WalletConnect ← Wallet connection button
│           │  ├─ DashboardHeader
│           │  │  └─ New Project / Join Session buttons
│           │  ├─ ProjectGrid
│           │  │  └─ ProjectCard[] ← Shows IPFS badge if saved
│           │  ├─ CreateProjectDialog
│           │  └─ JoinSessionDialog
│           │
│           └─ Editor.tsx
│              ├─ AppHeader
│              │  └─ EditorTopBar
│              │     └─ Save to IPFS button ← Triggers upload
│              ├─ FileSidebar (file tree)
│              ├─ CodeEditor (Monaco)
│              ├─ TerminalPanel
│              ├─ StatusBar
│              └─ SaveToIPFSDialog ← Upload UI
```

## Key Files & Their Roles

```
client/src/
│
├── lib/
│   ├── wagmi.ts
│   │   └─ Web3 config (chains, wallet providers)
│   │
│   └── ipfs.ts
│       └─ IPFS client & upload/retrieve functions
│
├── hooks/
│   └── useWalletAuth.ts
│       └─ Wallet signature logic
│
├── components/
│   └── web3/
│       ├── WalletConnect.tsx
│       │   └─ RainbowKit custom button
│       │
│       └── SaveToIPFSDialog.tsx
│           └─ Upload dialog with signature flow
│
└── pages/
    ├── Home.tsx
    │   └─ Dashboard with projects
    │
    └── Editor.tsx
        └─ Code editor with IPFS save

server/
└── routes.ts
    └─ API endpoints for signature verification
       and project metadata storage
```

---

This architecture provides:
- ✅ Decentralized storage (IPFS)
- ✅ Cryptographic authentication (wallet signatures)
- ✅ No passwords needed
- ✅ Permanent, immutable records
- ✅ Transparent ownership
- ✅ Censorship-resistant
